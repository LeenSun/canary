apiVersion: v1
kind: Service
metadata:
  name: app-ui-svc-blue
  labels:
    app: app-ui
    set: blue
spec:
  type: NodePort
  ports:
  - port: 80
    nodePort: 30001
    protocol: TCP
  selector:
    app: app-ui
    set: blue


